import React from "react";

import {Layout} from "../layout";

const Home = (props) => (
    <Layout>
        <h1 className='text-2xl'>Spa-sample Home</h1>
        <p>This is sample</p>
    </Layout>
);

export default Home;