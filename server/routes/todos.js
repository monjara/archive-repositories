'use strict';
const express = require('express');
const router = express.Router();

router
  .route('/')
  .get((req, res) => {
    // get handler
  })
  .post((req, res) => {
    // post handler
  });
